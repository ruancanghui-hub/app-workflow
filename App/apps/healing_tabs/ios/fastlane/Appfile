# Override via env or fastlane/.env (not committed).
app_identifier(ENV.fetch("APP_IDENTIFIER", "com.example.appTemplate"))
apple_id(ENV["APPLE_ID"]) if ENV["APPLE_ID"]
team_id(ENV.fetch("APPLE_TEAM_ID", ""))
