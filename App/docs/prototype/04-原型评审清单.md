# 松息原型评审清单

## 需求与范围

- [x] 每个 P0 需求有页面、状态、交互验收和边界。
- [x] 未加入睡眠分期、诊断、课程、故事、混音与专注。
- [x] 游客与权限拒绝仍可完成听声入睡闭环。

## 流程

- [x] 主闭环、返回、取消、离线、失败、权限拒绝和中断恢复均有出口。
- [x] 订阅页可关闭；购买失败不阻断免费闭环。
- [x] 睡眠进行中恢复与手动结束路径明确。

## 视觉与可用性

- [x] Direction 01 为唯一批准视觉源，今晚页保持原文件。
- [x] 四个根页固定为今晚、声音、睡眠、我的。
- [x] 本地原型使用一个共享 Tab 组件，仅切换激活态。
- [x] 390 × 844 基线可用；触控目标、键盘焦点与减少动态已覆盖。
- [x] 无医疗诊断和未经验证的睡眠分期文案。

## 工具与交付

- [x] 本地 HTML 只有一个 `data-codex-root`，静态节点 ID 唯一，资产本地。
- [x] 本地结构检查与需求包验证已定义。
- [ ] CDB 本地插件 preflight/preview/send：当前运行环境未提供该工具，原型 Gate 保持 PENDING，不能伪造通过。
- [ ] 12UI Branch：CLI 已安装但未登录；已记录授权链接，未将此项冒充完成。

## 评审结论

产品、信息架构、状态覆盖和本地原型可进入评审；正式 Phase 3 Gate 仍需在可用环境补跑 CDB 本地插件流程。

## 本地验证证据（2026-08-28）

- `validate_core_tab_ui.py`：PASS；四张 853 × 1844，首页 SHA-256 与批准源一致。
- `validate_prototype_package.py App`：`Prototype package is valid!`
- `validate_handoff.py`：PASS。
- 浏览器 430 × 900 视口：四个 Tab 均可见；`root-tabbar` 实例数为 1；切换前后 bounding rect 相同；标签顺序为今晚、声音、睡眠、我的。
- 浏览器主闭环：声音 → 松林细雨 → 开始睡眠 → 结束并查看报告 → 睡眠，全部可达。
- 权限拒绝状态：展示“权限已拒绝，仍可听声入睡”，未阻断根导航。
