# Context terms for asset UI prototype

Add these to `output/brand-ip/<slug>/CONTEXT.md` when running `composing-asset-ui-prototype`:

| Term | Definition sketch |
| --- | --- |
| 视觉契约 | `04-core-tab-ui/` QA-passed reference PNGs |
| 切图资源 | `05-ui-assets/<tab>/` grouped PNGs + manifest |
| 资产合成原型 | `06_asset_ui/` HTML composite |
| 根 Tab | Fixed bottom nav set from PRD |
| 激活态 | Current Tab highlight in that page's `nav_icons/` |
| 设计坐标系 | Reference canvas pixels (default 941×1672) |
| 语义角色 | `manifest.json` → `source_role` |

Keep `CONTEXT.md` glossary-only — no implementation details.
