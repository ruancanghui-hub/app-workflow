# 原型评审准则

- 需求：每个 MVP 需求都有页面、状态和验收项；非目标没有被加入。
- 流程：主闭环、返回、取消、失败、权限拒绝、离线和中断均有出口。
- 视觉：首屏一个主要行动；信息层级、文本对比、触达区域和 390 × 844 裁切可用。
- 健康：明确非医疗，展示来源/覆盖率/可信度，敏感权限分项说明，删除可达。
- CDB：每页一个 root，节点 ID 稳定唯一、资产本地、预检通过、关键状态已预览。
